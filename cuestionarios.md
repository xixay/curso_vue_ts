# Examen Teórico Vue 3

**Estado:** Terminado  
**Calificación:** 9.00 de un total de 10.00 (90%)

---

### Pregunta 1
**¿Es verdad que Vue Router se encarga del manejo de las rutas del lado del cliente?**

* Verdadero 
* Falso

> **Respuesta correcta:** Verdadero

---

### Pregunta 2
**Se quiere declarar e inicializar una variable de estado, llamado `count = 0` de tipo numérico, Escriba la línea de código a continuación.**

> **Respuesta correcta:** `const count = ref(0);`

---

### Pregunta 3
**¿Qué directiva Vue me permite trabajar con el atributo de una etiqueta de manera dinámica?**

* a. `v-model`
* b. `v-on`
* c. `v-bind` 

> **Respuesta correcta:** c. `v-bind`

---

### Pregunta 4
**¿Es verdad que Vue tiene la propia definición en archivos (`.vue`) para el manejo de componentes?**

* Verdadero 
* Falso

> **Respuesta correcta:** Verdadero

---

### Pregunta 5
**Qué comando en consola me permite ejecutar y visualizar mi aplicación Vue generada por Vite en un navegador.**

* a. `pnpm run serve`
* b. `npm run server`
* c. `pnpm dev`
* d. `pnpm run dev` 

> **Respuestas correctas:** `pnpm run dev`, `pnpm dev`

---

### Pregunta 6
**En el ciclo de vida de una instancia Vue, se desea obtener información de un servicio remoto, ¿qué parte de este ciclo de vida me permite realizar esta operación? Sabiendo que se quiere traer la información cuando el componente está dibujado en el DOM, y ya se tenga el modelo de datos, métodos y demás definido.**

* a. `onUpdate()`
* b. `onMounted()` 
* c. `unMounted()`

> **Respuesta correcta:** b. `onMounted()`

---

### Pregunta 7
**Escriba el comando correcto para realizar la instalación de "Axios" (Cliente HTTP para Nodejs) como dependencia que acompañara al producto final. Suponiendo que se está apuntado en la consola al proyecto de forma correcta.**

> **Respuesta correcta:** `pnpm install axios` *(Respuesta introducida: `pnpm add axios` - Incorrecta)*

---

### Pregunta 8
**¿Vue como framework para aplicaciones web de una sola página, define el concepto de "Single File Component" para hacer referencia a estructuras que definen parte o porciones de (vista, lógica y estilos)?**

* Verdadero 
* Falso

> **Respuesta correcta:** Verdadero

---

### Pregunta 9
**¿Qué directiva Vue permite realizar el enlace reactivo de una propiedad del modelo de datos con una entrada de datos de la vista, asegurando el flujo de entrada y salida en ambos sentidos?**

* a. `v-model` 
* b. `v-show`
* c. `v-bind`
* d. `v-on`

> **Respuesta correcta:** a. `v-model`

---

### Pregunta 10
**Para manejar la protección de rutas previa redirección al componente. ¿Existe un método con 2 parámetros (`(to, from) => {}`) que permite evaluar el acceso a una ruta en base a una condición?**

* Verdadero 
* Falso

> **Respuesta correcta:** Verdadero
